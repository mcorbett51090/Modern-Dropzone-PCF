# PCF Customization Rules
- Always preserve backward compatibility
- Prefer adding new input properties over breaking existing ones
- Default new boolean properties to "True" for SharePoint-only mode
- Bump version and add "-custom" suffix
- Document every change with comments
